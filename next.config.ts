import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export', // Enables static site export
  basePath: '/omdmhd.github.io', // Replace with your GitHub repo name
};

export default nextConfig;
